# Copy to .env.local for local dev; on Cloudflare Pages set these as build env vars.
# Both are PUBLIC/bundle-safe (Hard Rule 3): the publishable key is gated by Auth + RLS.
# The secret key (sb_secret_...) must NEVER appear here, in the bundle, or in git.

VITE_SUPABASE_URL=https://gwaxsksjierpzbxugbxj.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_f6xnKDgjeStNYV-H19VPtg_WqOcwiCw
