# Viewer app
Static app: Supabase Auth login, lists dives, downloads CSV via signed URL, renders charts
by reusing the firmware portal chart renderer (parseCsv / miniChart / drawCharts from
firmware/src/portal_page.h). To build once cloud schema is live.
