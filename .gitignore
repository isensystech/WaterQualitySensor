# ── PlatformIO ──────────────────────────────────────────────────────────────
.pio/

# Build artifacts / work builds. Versioned release bins are published as GitHub
# Release assets (git tag v<ver>), NOT committed -- see CLAUDE.md "builds".
builds/
*.bin
*.elf
*.map

# ── Editor: VS Code ─────────────────────────────────────────────────────────
# Keep .vscode/extensions.json tracked (recommends the PlatformIO extension);
# everything below is per-machine / generated.
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch/
.vscode/settings.json

# ── OS junk ─────────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# ── Misc / temp ─────────────────────────────────────────────────────────────
*.swp
*.swo
*~
*.bak
*.tmp
*.log
*.zip
