# secrets — never commit
.env
.env.*
*.secret
service_role*
sb_secret*
cloud/supabase/.temp/
# node / python cruft for cloud+basestation
node_modules/
__pycache__/
*.pyc
.venv/

# Supabase CLI junk when run from repo root (real config lives in cloud/supabase)
/supabase/
